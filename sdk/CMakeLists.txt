# ==========================================================================
# sdk/CMakeLists.txt
# --------------------------------------------------------------------------
# SDK sources are compiled directly into the mbootcore target.
# See lib/CMakeLists.txt for the MBOOTCORE_SOURCES list.
# This file exists only for IDE navigation — no library target here.
# ==========================================================================
