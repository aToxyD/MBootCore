# ==========================================================================
# lib/src/security/mbedtls/CMakeLists.txt
# --------------------------------------------------------------------------
# Mbed TLS provider — sources are compiled directly into mbootcore.
# Include paths and linking are set in lib/CMakeLists.txt.
# ==========================================================================

# Source files are conditionally added to MBOOTCORE_SOURCES in
# the parent CMakeLists.txt.
