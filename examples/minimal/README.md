# Minimal Example

Demonstrates device detection and connection using the low-level `Session` API.

This is the simplest possible MBootCore program — connect, detect, disconnect.

Build with: `cmake -DMBOOTCORE_BUILD_EXAMPLES=ON ..`
Run with: `./example_minimal`
